/*function soma(n1, n2){
    return n1 + n2
}
console.log(soma(2, 5)) // escrever na tela a soma de 2 e 5
*/

/*function soma(n1, n2){
    return n1 + n2
}
console.log(soma(2)) // Nesse caso a resposta é NaN, pois ficou 2 + undefined e isso é NaN pro JS.
*/

/*function soma(n1=0, n2=0){ // considera 0 caso não passe o valor pra um dos parâmetros.
    return n1 + n2
}
console.log(soma(7, 3))
*/

